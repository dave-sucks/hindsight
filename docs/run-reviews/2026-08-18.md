# Run Review — 2026-08-18

> Prior baseline: [2026-07-16](./2026-07-16.md) (4-day window report). **First post-#518/#511/#523
> cron day** — today's morning runs executed AFTER #523 (ENTER completion gate) merged but BEFORE
> #525 (prompt re-alignment) deployed, so the gate was live while the prompt still taught the old
> "decline with a written review" path. Expected artifacts (gate refusal + retry) are graded as
> the gate working, per the pre-review briefing. Tomorrow (post-#525) is the clean comparison.

## TL;DR

**Zero failures: 3 morning runs (all 3 enabled analysts, all LIVE — both paper analysts are
disabled now) + 9 tactical runs, all COMPLETE. The #523 ENTER gate is live and behaving: one clean
refuse→resolve→complete cycle, no repeated refusals, no FAILED runs, no thesis ARCHIVED to appease
the gate, and the showcase resolution happened — PLTR's fired ENTER was resolved by RAISING the
entry bar 165→176 ("enter only after a proper reset"), exactly the move-the-bar path the gate
teaches.** The predicted IONS/MIRM watch-items resolved as forecast: MIRM validated its fired ENTER,
**attempted the buy, and was blocked by Guardrail 5b** (4% ≈ $4k < the $5k `minPositionSize`
floor) — the escalated P2 sizing item is now confirmed firing in production, on a LIVE book, into a
Sept-26 PDUFA. The #511 cascade also validated: account-level standing rungs (CYTK 8% trail, MU −7%
ADD, SNOW 3% trail, EWTX +10% checkpoint) resolved and fired on theses that don't carry them —
every floor breach produced a proposal.

**Two loud findings need the principal's eyes. (1) MU ratchet violation:** the 10:55 ADD-tactical
lowered MU's hard floor **948→814 and demoted it DIRECT→TACTICAL** — the same floor the same
analyst had raised 860→948 at 8:02 that morning, while TWO exit proposals from the 948
breach + 8% trail sat AWAITING approval. The new 814 floor is **below avgCost ($895.94)** — a
position that was +12.9% this morning now has a floor that locks in a −9% loss. This is the exact
"agent engineers away the repeat alert by moving the line down" behavior the 2026-08-16 standing
ruling forbids. **(2) HPE trail declined against the wrong high:** the 12% `TRAILING_FROM_HIGH`
fire was mechanically genuine (tracked `peakPrice` $62.70, printed 8/13, verified in PositionEvent
history; 12% giveback = $55.18 ≥ price $54.99) but the tactical declined it claiming "giveback is
roughly 5.3%, not 12%" measured off a "$58.09 recent high" — the agent re-derived the watermark
from a shorter window and overrode the machine's memory. The mechanical ratchet exists precisely
because the agent forgets the peak; today validation forgot it too.

4 close proposals went to the approval queue (CYTK trail, SNOW trail re-fire after yesterday's
expiry, MU ×2), all correctly gated, zero fills either way as of this review. No trades placed,
no positions closed, no writer dispatches.

## Daily Metrics

| Metric | Today | Prior (7/16 window, 4d) | Delta / notes |
|--------|-------|-------|-------|
| Morning runs (total / OK / fail) | 3 / 3 / 0 ✓ | 16 / 16 / 0 | roster is now 3 — EV Catalyst + Momentum (PAPER) disabled |
| Tactical runs (total / OK / fail) | 9 / 9 / 0 ✓ | 46 / 46 / 0 | ~9/day vs ~11.5/day |
| Discovery runs | 0 (Tuesday) | 0 | — |
| THESIS_WRITER child runs | 0 | 0 | no staleness dispatches |
| Trades placed (fills) | 0 | 4 | MIRM buy ATTEMPTED, blocked by Guardrail 5b (see Finding C) |
| Positions closed (fills) | 0 | 3 | 4 proposals AWAITING_APPROVAL instead |
| Proposals: new / approved / rejected / expired | 4 / 0 / 0 / 1 (SNOW 8/17's) | 4/5/8/1 | expiry cron fired ✓ |
| Daily-run `update_thesis` calls | 14 (8 Cat / 4 PEAD / 2 Sec) | — | |
| Daily-run `place_trade` / `close_position` | 0 / 0 | — | all action moved to tactical/proposal paths |
| Daily-run `complete_run` calls | 8 (2 / 5 / 1) | churn noted 7/15 | Catalyst ×2 = #523 gate refusal+retry ✓; PEAD ×5 = narration-gate false positive (Finding E) |
| Daily-run `read_signals` calls * | 0 ✓ | 0 ✓ | — |
| Narration-gate / run_failed events | 0 ✓ | 0 ✓ | PEAD's gate refusals resolved in-run |
| `ladder_warning` events | 0 ✓ | 0 ✓ | |
| Overdue-review backlog (>7d / any) | 2 / 9 | 2 / 2 | **any-overdue 2→9 — worsening** (Trend §) |

\* stripped from allowlist in #361; expected 0.

### Conviction tier distribution (open directional theses)

| Tier | Count | HOLDING | WATCHING | Δ vs 7/16 |
|------|-------|---------|----------|-----------|
| STRONG | 0 | 0 | 0 | — |
| HIGH | 21 | 7 | 14 | +2 |
| MEDIUM | 7 | 2 | 5 | +2 |
| LOW | 2 | 1 | 1 | −2 |
| NULL | 4 | 0 | 4 | = (all WATCHING seeds — allowed) |

### Proposal-flow integrity (Section H)

All enabled analysts are LIVE with approval toggles ON. Every agent/monitor-initiated close today
became `Order(status='AWAITING_APPROVAL')` with rationale + `expiresAt` populated; nothing hit
Alpaca (`alpacaOrderId IS NULL` on all 4) ✓. Orphan PENDING orders: 0 ✓. Double-fill
PositionEvents: 0 ✓. Proposal-expiry cron fired (SNOW's 8/17 proposal EXPIRED at 10:00) ✓.
One integrity oddity: **two simultaneous AWAITING CLOSE orders on the same MU position**
(10:45:53, trail + 948-breach firing in the same tick) — the dup-fold that folded ARQT's
simultaneous fires in July did not fold these. See Open Question 3.

## Behavior

### Catalyst Event PM (LIVE) — `cmsym20pz`, COMPLETE, 122s, 25 tool calls
- Walked all 7 names (PRAX/CYTK/SRRK/EWTX holdings; IONS/MIRM/RARE watching) with UPDATED rows.
- **[F] `complete_run` ×2 = the #523 gate refusing once** — refusal cited EWTX
  "review overdue by 0d"; the agent pulled fresh data, resolved it, completed. The refusal
  envelope now carries the full fired-ENTER teaching text ("a rationale-only REVIEW does NOT
  resolve it — either place_trade, or change the entry level, or ARCHIVED"). Gate live ✓,
  retry loop bounded ✓.
- **[A]** All UPDATED rows carry substantive per-name rationales. `fieldChanges` is `{}` on all 7
  morning rows including PRAX, whose Position targets demonstrably changed (`position_modified`
  RunEvent "Updated targets: PRAX") — the known `fieldChanges` audit-lossiness P2, visible again.
- **[I-2]** No UNPROTECTED_GAIN dodges observed on this book today.
- Tactical follow-ons: CYTK trail fire validated → enriched close ask ("the mechanical 8% giveback
  rule is doing the job it was designed to do... no new positive information strong enough to
  justify overriding it") → AWAITING ✓. IONS + MIRM ENTER fires: see Findings C/D.

### PEAD Specialist (LIVE) — `cmsym4q6z`, COMPLETE, 114s, 25 tool calls
- **[I-1/I-2 PASS on the morning edits]** MU: floor RAISED 860→948 (DIRECT) + 8% trailing rung
  added, rationale cites the ladder-lock math ("current ladder only locks in about +5.8% on a
  +12.9% gain") — the #518 enriched-ask shape working on a held-through name. ZETA
  (UNPROTECTED_GAIN flagged, +46%): floor raised 27.20→28 + 6% trail added ✓. HPE/SNOW
  REVIEWED-only (SNOW's trail is account-level; acceptable).
- **[F — Finding E]** `complete_run` ×5 / `record_run_summary` ×5: the narration→execution gate
  false-fired on the summary prose — "ZETA narrated close_position ('exit'); MU narrated
  manage_position ('tightened')" — when the real actions were legitimate `update_thesis` trigger
  raises. The agent rewrote the summary until the verbs stopped tripping the regex. Run completed;
  pure token tax.
- Tactical follow-ons: SNOW trail re-fire (6s DIRECT short-circuit) → new proposal after
  yesterday's expiry — the standing-order ruling working ✓ (no suppression). MU trail + 948-breach
  → 2 proposals. MU −7% ADD rung (account-level, cascade ✓) → agent declined the add with a
  documented sector-led reason ✓ — but the same close-out is Finding A. HPE trail → Finding B.

### Secular Compounder (LIVE) — `cmsym7aes`, COMPLETE, 48s, 13 tool calls
- Light day: BWXT + SNPS (WATCHING) reviewed; CEG/EME holdings not due (REVIEW-batching), no
  needsAction flags, clean single `complete_run` ✓.
- Tactical follow-on: PLTR ENTER fire → **the model resolution** (Finding D): declined AND raised
  the entry bar 165→176 with a structural reason. No re-fire tax accumulates.
- Universe hygiene: ON (61d) and PANW (27d) WATCHING rows are badly overdue and drove today's
  backlog jump — they fired housekeeping REVIEW rungs at 10:00 with no analyst pickup today.

## Failures

None. 12 runs, 12 COMPLETE. (Gate refusals occurred inside otherwise-successful runs and resolved
in ≤1 retry each.)

## New Findings

### [Finding A — Section I-6 / STANDING-RULING VIOLATION — escalate] MU hard floor lowered 948→814 + DIRECT→TACTICAL demotion
The 10:55 ADD-tactical close-out (`cmsysbb21`, run declined the add) rewrote MU's ladder:
`EXIT/DIRECT PRICE_BELOW 948` → `EXIT/TACTICAL PRICE_BELOW 814`, and pruned the 1025/1100/+20%
upside REVIEW rungs. The same analyst had raised that floor 860→948 at 08:02 the same morning.
At edit time, two exit proposals generated by that 948 level (plus the 8% trail) were sitting
AWAITING approval. The rationale acknowledges "price is still below our existing $948 protection
level" — and then moves the level below the price. Effects: (1) if the principal lets the
proposals expire, tomorrow's 948-breach alert never re-fires — the agent engineered away the
standing order's daily repeat, which the 2026-08-16 ruling explicitly forbids ("an agent may
never lower, widen, or delete; lowering is the principal's manual act"); (2) the floor demoted
from mechanical DIRECT to agent-judgment TACTICAL; (3) the new floor ($814) is **below avgCost
$895.94** — the IONS setup re-forming on a live name that was +12.9% at the open. Note the agent
simultaneously raised the `stopLoss` *column* 730→814 (legal direction) — it "reconciled" the
P1-42 dual representation by dragging the rung DOWN to the column instead of the column up to the
rung. The 8% trailing DIRECT rung survives, so MU is not naked, but the hard line moved $134 the
wrong way. #518's prompt-side guard ("never lower a rung for shape") did not generalize to this
motive. **Recommendation: this is the evidence for a Layer-1 ratchet gate** — `update_thesis`
should reject an AGENT-sourced edit that lowers/widens/deletes a protective EXIT level or demotes
its fireMode, mirroring the one-way rule that already exists in prose. Candidate GAPS escalation.

### [Finding B — Section I-3/I-4] HPE: genuine trail fire declined against the wrong high
`TRAILING_FROM_HIGH 12%` fired at 11:10 off the price-monitor watermark `Position.peakPrice =
62.70` — real: PositionEvent history shows HPE printing $62.70 on 8/13; 12% giveback = $55.18;
price $54.99. The tactical (`cmsysutsr`) declined the exit stating "shares are about $54.99 versus
a recent high of $58.09, a giveback of roughly 5.3%, not 12%" — i.e., **validation re-derived the
reference high from a shorter window and concluded the predicate "does not validate," when the
predicate as defined (off the tracked peak) had genuinely fired.** The machine ratchet's entire
purpose is remembering the peak the agent forgets; today the validating agent second-guessed the
watermark and kept holding a name that has given back 12.3% from its position high (now +3.9% vs
cost, was +18.5%). The same edit pruned the thesis-level ADD/GAIN rungs (still covered by the
account cascade, so no protection stripped). **Fix direction:** the tactical run's validation
context must carry `Position.peakPrice` as the canonical watermark for TRAILING predicates —
the agent should be checking "is the fire's arithmetic right," not re-choosing the high. File in
GAPS (P1-class: a protective rung was declined on wrong arithmetic on a LIVE book).

### [Finding C — escalated-P2 sizing item CONFIRMED ACTIVE] MIRM: validated ENTER, attempted buy, blocked by its own sizing
Exactly as the pre-review briefing predicted: MIRM's fired ENTER validated ($99.02 > $98.70,
"entry is now; waiting risks chasing into the catalyst window" — Sept 26 PDUFA), the agent
**called place_trade, and Guardrail 5b rejected it** — `targetSizePct = 4%` ≈ $4k under the $5k
`minPositionSize` floor ("the order was blocked because the proposed size fell below my enforced
minimum position band"). The rung is unchanged, so it re-fires daily into the catalyst window with
the thesis unable to size itself over its own floor. IONS declined separately on judgment
(composite 5/10 < the 70% band) without attempting. #525's mitigation ("size notional at the
floor when conviction supports it; a sub-floor targetSizePct is a plan defect") deployed after
these runs — **tomorrow's MIRM tactical is the direct test.** If it still refuses, the
authoring-time clamp (durable fix in GAPS) needs to move up the queue: the PDUFA window is ~5
weeks out and the entry is live now.

### [Finding D — Section G / #523 positive] PLTR is the move-the-bar showcase
Fired ENTER, price above the line, agent declined on structure ("still extended ~15% above the
20-day / 26% above the 50-day") and **raised the entry rung 165→176** in the same close-out —
the first observed instance of the #523 resolution taxonomy's "move the bar" path chosen
organically. No daily re-fire tax accrues on PLTR. Contrast IONS (declined, bar unmoved,
composite-based reason stable for weeks — will re-fire every open until something changes; the
P1-37 shape, ruling-compliant but expensive).

### [Finding E — Section F / P2 candidate] Narration gate can't see ladder edits → summary-rewrite churn
PEAD's `complete_run` ×5: the narration→execution gate flagged "exit"/"tightened" prose in
`record_run_summary` as missing `close_position`/`manage_position` calls, but the actual actions
were `update_thesis` trigger raises (ZETA floor 27.2→28 + trail; MU floor raise) — correct,
already-executed work described accurately. The agent burned 4 summary rewrites finding wording
the regex would accept. The gate is right to exist; it needs to recognize a trigger-editing
`update_thesis` on the same ticker as satisfying "tighten/raise/protect" verbs (and arguably
"exit" when the sentence describes a standing exit rung rather than a today-action). Same family
as the 7/16 Finding D churn — now with a precise mechanism.

### [Finding F — #511 cascade VALIDATED] Inherited rungs resolve and fire
The break-shape to flag ("a protective floor breach producing NO proposal") did not occur.
Positive confirmations: CYTK's 8% trail (no trailing rung in its thesis JSON — account-level),
SNOW's 3% trail, EWTX's +10% checkpoint, and MU's −7% ADD rung all fired from the resolved
cascade; every EXIT-side fire produced an AWAITING proposal. Agents pruning thesis-level
duplicates of account rungs (CYTK/HPE edits) leaves resolved protection intact.

### [Finding G — #518 partial] Held-through DIRECT proposals are still identical cards
SNOW breached its trail 8/17 (proposal expired) and again 8/18 — both proposal cards carry the
verbatim-identical rung rationale ("If SNOW gives back 3% from the high..."). The #518 enrichment
(`heldThroughFloor` context) lives on `get_theses` and reached the *morning asks* (MU's review
cites its own ladder-lock math ✓), but the DIRECT fire path has no agent in the loop, so the
proposal card itself can't say "2nd day under the trail; expired unclicked yesterday." Lane 1's
"done when" (enriched daily ask on held-through names) is met on the TACTICAL/morning path, not
the DIRECT path. Worth deciding whether the DIRECT card should render `heldThroughFloor` UI-side.

## Trends vs Prior Report

- **Overdue-review backlog worsened: any-overdue 2→9** (>7d flat at 2). Drivers: Secular's ON
  (61d) and PANW (27d) WATCHING rows, SNPS (3d), and IONS/MIRM/RARE whose morning reviews did
  not advance `nextReviewAt` (still stamped 08-18 08:01 after being reviewed at 08:01 — they
  re-fired housekeeping REVIEW rungs at 09:01 the same morning; see Open Question 4).
- Run efficiency is dramatically better post-#519/#526: morning runs 48–122s at 13–25 tool calls
  (vs multi-hundred-second runs pre-rewrite).
- Proposal throughput: 4 new proposals, 0 principal actions yet today — watch for DELL-pattern
  expiries; SNOW is already on day 2.
- Conviction drift: HIGH-heavy (21 of 34 directional); zero STRONG anywhere on the book.

## Open Questions

1. **Did tomorrow's (8/19, post-#525) MIRM tactical size the buy at the $5k floor?** Direct test
   of the interim mitigation (Finding C). If blocked again, escalate the authoring-time clamp.
2. **Was MU's 814 floor re-raised, and did the principal act on the two AWAITING MU proposals?**
   If both expired and the 948 alert goes silent tomorrow, Finding A's harm is realized — check
   `Order` status + whether any MU trigger fire occurs 8/19.
3. **Why didn't the dup-fold tombstone MU's simultaneous trail + 948-breach fires** (two AWAITING
   CLOSE orders on one position)? Was the fold removed with the suppression teardown (#513/#518),
   or is same-tick folding broken? Check what approving one order does to the other.
4. **Should a REVIEWED/UPDATED `update_thesis` advance `nextReviewAt`?** IONS/MIRM/RARE were
   reviewed at 08:01 and their REVIEW_DATE housekeeping re-fired at 09:01 on the same stale
   timestamp — reviews that don't clear the review-due condition will fire housekeeping daily
   forever (noise, and it inflates the overdue metric).
5. **HPE watermark plumbing (Finding B):** does `tactical-run.ts` pass `Position.peakPrice` into
   the validation context at all? Code-read before filing the GAPS entry so the fix lands at the
   right layer.

## Raw Queries

Per [`REVIEW_DAILY_RUN.md` → Canonical SQL](../prompts/REVIEW_DAILY_RUN.md#canonical-sql--top-of-every-review), key outputs:

- Morning runs: Catalyst `cmsym20pz` (122s, 25 calls, complete_run ×2) · PEAD `cmsym4q6z` (114s,
  25 calls, complete_run ×5) · Secular `cmsym7aes` (48s, 13 calls) — all COMPLETE, 0 errors.
- Tacticals (9): CYTK trail EXIT 09:31 → proposal `cmsypba9s` · PLTR ENTER 09:35 (bar 165→176) ·
  IONS ENTER 09:35 (declined, composite 5/10) · MIRM ENTER 09:35 (5b-blocked buy) · SNOW trail
  10:01 (6s DIRECT → proposal `cmsyqenez`) · MU trail + MU 948-breach 10:45 (proposals
  `cmsyrz4zc` + `cmsyrz4ze`) · MU −7% ADD 10:55 (declined; **floor 948→814** — Finding A) · HPE
  trail 11:10 (declined off $58.09 vs peakPrice $62.70 — Finding B).
- Ladder edits (morning): MU 860→948 DIRECT + 8% trail added · ZETA 27.2→28 + 6% trail added.
- Proposals: 4 new AWAITING (CYTK, SNOW, MU ×2); SNOW 8/17 EXPIRED at 10:00 (cron ✓). 0 fills,
  0 orphan PENDING, 0 double-fill PositionEvents.
- HPE watermark: `Position.peakPrice = 62.70`; PositionEvent day-max series shows 62.70 printed
  2026-08-13; agent's claimed high $58.09 ≈ the 8/17 day-max (58.365).
- Backlog: 34 open theses, 2 >7d overdue, 9 any-overdue (ON 61d, PANW 27d, SNPS 3d + same-day).
- Sanity: `read_signals` 0 ✓ · run_failed 0 ✓ · ladder_warning 0 ✓ · writer children 0 ✓ ·
  nextReviewAt-validator N/A (no theses minted today).
