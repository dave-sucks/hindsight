# Run review — Wednesday 2026-09-09

Reviewed at 11:40 ET against the production database. Method: the twelve
invariants in `docs/prompts/RUN_REVIEW_INVARIANTS.md` (written today), then the
morning runs, the tacticals, and every fix claimed in the last three weeks.

## What ran

| Analyst | Run | Result | Writers dispatched |
|---|---|---|---|
| Catalyst Event PM | 08:00–08:05 | COMPLETE, 7 names, 0 trades, 5 audit rows | CYTK, BMRN, AGIO — all landed |
| PEAD Specialist | 08:05–08:10 | COMPLETE, 5 names, 0 trades, 2 audit rows (HWM, TOST archived) | TOST — landed, then archived 13 s later |
| Secular Compounder | 08:10–08:11 | COMPLETE, 8 names, 0 trades, 5 audit rows | none |

Tacticals: SRRK 09:50 (stop $56.40 and 8% trail both fired → sell proposed,
duplicate auto-rejected), PBH 10:00 (8% trail → sell proposed). SMMT and MU
review wakes deferred to the next daily run. Three writer mints (DOCU, HPE,
FIVE) running at 11:35 from a principal-chat session.

Every thesis write today landed. Zero database errors. Writer "Data pulled"
lines are honest: eleven "all sources returned data", one named empty
(ASML `financials(empty)` — foreign filer, no US-GAAP filings on Finnhub).

## Confirmed fixed (evidence, not PR descriptions)

| Claim | Evidence today | Status |
|---|---|---|
| Buy triggers fire on the crossing (#590) | VST/ABT/CYTK each fired once on 09-08; none re-fired 09-09 | ✅ |
| 2:1 floor on explicit level edits (#600) | Three refusals this morning: CYTK 0.85, TOST 1.47, VST 1.36 | ✅ (but see gap 1) |
| Writer can write "no entry yet" (#600) | BMRN, CSCO, PLTR, SYK on the book unpriced | ✅ |
| Schema matches database (#609) | 0 P2022 events; all writes landed | ✅ |
| Writer fails honestly (#609) | SMMT verify run: "Persist refused", FAILED, 0 rows | ✅ |
| FMP → Finnhub (#611) | No FMP calls; statements present on 11 of 12 pulls | ✅ |
| Held refresh clamps a looser stop (#612) | SMMT second run landed | ✅ |
| Held names re-researched | 8 of 8 landed overnight | ✅ |
| Scale-in rungs off watch rows (#597) | No ADD fire on WATCHING since 09-03 (PLTR still stores one; read side drops it) | ✅ |
| Fill keeps analyst ladder (#599) | ASML has target $2,800 + exit rungs | ✅ |
| Sizing settings (#605/#606) | 5k/8k/16k · 7k/14k/28k · 10k/10k/20k | ✅ |
| Position ↔ thesis sync | 8 open = 8 HOLDING, 0 orphans | ✅ |
| Same-bucket double spawn (#602) | none | ✅ |

## Gaps found today

1. **The 2:1 gate skips triggers-only edits.** `update_thesis` validates the
   plan only when the agent passes `entry_price`/`target_price`/`stop_loss`. A
   resend of `triggers` moves the buy level through derive-on-write and never
   runs the floor. ETN this morning: buy moved $375 → $432, target $490 and stop
   $355 kept, **0.75:1 stored**. Fix: validate the derived tuple (PR from this
   session). Friday's run will get ETN as forced work via `PLAN_BELOW_RR_FLOOR`.
2. **Research bought, then thrown away.** PEAD dispatched a $-costing TOST
   refresh (landed 2.91:1 at 08:09:35), tried to overwrite it with a 1.47:1 plan
   (refused), then **archived** TOST at 08:09:48 — with a rationale that says
   "keeping only a time-based review wake", i.e. it meant to set the plan down,
   and reached for ARCHIVED instead. Third time TOST has been killed. After #613
   the right landing (a watch with no clock) exists; the daily-run prompt should
   name it as the alternative to archiving.
3. **Discovery has not run since 2026-05-31.** Sunday 09-06 produced nothing.
   The cron is registered in code (`0 9 * * 0` ET). Every new name on the book
   since June came from principal-chat dispatches. Needs the Inngest dashboard
   (paused function?). Open on DAV-231 since 09-02.
4. **VST is still a plan that cannot fill.** Composite 5/10 vs Compounder's
   minimum 70. The flag reached the agent (its rationale cites the "subscale
   thesis score"); it raised the target and kept the buy trigger. Next crossing
   is refused again. The input worked; the decision didn't.
5. **Two sell rungs, two tactical runs, two orders.** SRRK's stop and its 8%
   trail fire together (09-08 and 09-09); the second order is auto-rejected but
   the run is paid for. #602 covered same-bucket only.
6. **Trigger text ≠ number, live example.** MU's exit rung is at $969 and its
   sentence says "Exit below $935". DAV-231 open bullet.
7. **Sold-name leftovers cost reviews.** PRAX (sold) was reviewed by Catalyst
   today as a full plan; EME carries the dead position's plan and an EXIT rung.
   DAV-228 / DAV-240.
8. **Update receipts have no ticker.** Every `update_thesis` GateRejection row
   has `ticker = null` (the tool takes `thesis_id`). Unreadable by stock.
9. **Foreign filers have no statements on Finnhub.** ASML (held) researched
   without financials. Named honestly now; needs a source or an explicit
   "ADR: statements unavailable" in the writer prompt.

## Needs Dave

- **SRRK**: stop $56.40 breached since 09-02; a sell has been proposed five
  trading days running and rejected or expired each time. Either sell, or edit
  the stop below the price so it stops proposing (only a person may lower it).
- **PBH**: 8% trail fired; sell awaiting approval.
- Inngest dashboard: is `discovery-run` paused?
- Compounder smallest trade = largest trade = $10k (you said $8k floor).

## Not verified here

Header P&L (#604), UI surfaces, stale-quote logging — need a browser or Vercel
logs, not the database.
