Read these files in order before doing anything else:

1. `CLAUDE.md` — stack, architecture, agent tools, design rules, recurring bugs
2. `docs/PRINCIPLES.md` — three-layer principle: which layer each rule belongs in (tool gate vs tool result vs prompt)
3. `docs/THESIS_ARCHITECTURE.md` — the thesis state machine, producers, gates, lifecycle scenarios
4. `docs/GAPS.md` — P0 list only (skip P1/P2 unless the task explicitly touches them)

After reading, briefly confirm what you understood and what task we're working on.
